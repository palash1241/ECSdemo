family: testecs
networkMode: awsvpc
requiresCompatibilities:
  - FARGATE
cpu: 256
memory: 512
containerDefinitions:
  - name: testecs
    image: placeholder
    essential: true
    portMappings:
      - containerPort: 80
        protocol: tcp


